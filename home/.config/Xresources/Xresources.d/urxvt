#ifdef LODPI
#  define FONT_SIZE 14
#elif defined(MEDDPI)
#  define FONT_SIZE 20
#else
#  define FONT_SIZE 25
#endif

URxvt*font: xft:Ubuntu\ Mono\ derivative\ Powerline:pixelsize=FONT_SIZE:antialias=true:hinting=full,xft:DejaVu\ Sans\ Mono\ for\ Powerline:pixelsize=FONT_SIZE:antialias=true:hinting=full
URxvt*boldFont: xft:Ubuntu\ Mono\ derivative\ Powerline:style=bold:pixelsize=FONT_SIZE:antialias=true:hinting=full,xft:DejaVu\ Sans\ Mono\ for\ Powerline:pixelsize=FONT_SIZE:antialiaz=true:hintig=full
